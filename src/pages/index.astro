---
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/Hero.astro';
import SectionHeader from '../components/SectionHeader.astro';
import BlogCard from '../components/BlogCard.astro';
import ProjectCard from '../components/ProjectCard.astro';
import { getCollection } from 'astro:content';

const allPosts = await getCollection('blog');
const latestPosts = allPosts
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);

const allProjects = await getCollection('projects');
const featuredProjects = allProjects
  .filter(p => p.data.featured)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 4);

const personJsonLd = JSON.stringify({
  "@context": "https://schema.org",
  "@type": "Person",
  "name": "Shamyl Bin Mansoor",
  "url": "https://shamylmansoor.com",
  "image": "https://shamylmansoor.com/images/shamyl.jpg",
  "jobTitle": "Co-founder & CTO",
  "worksFor": {
    "@type": "Organization",
    "name": "LearnOBots",
    "url": "https://learnobots.com"
  },
  "sameAs": [
    "https://x.com/shamylmansoor",
    "https://linkedin.com/in/shamylbinmansoor",
    "https://github.com/shamyl",
    "https://hackaday.io/shamyl"
  ]
});
---

<BaseLayout title="Home" description="Shamyl Bin Mansoor — Co-founder & CTO of LearnOBots. Robotics entrepreneur, maker, and STEAM educator building products that matter in Pakistan." jsonLd={personJsonLd}>
  <Hero />
  
  <!-- Featured Work Section -->
  <section class="section">
    <div class="container">
      <SectionHeader 
        title="Featured Work" 
        subtitle="Selected projects across software, hardware, and research"
        link="/work"
        linkText="View All Projects"
        figNumber="01"
      />
      <div class="projects-grid">
        {featuredProjects.length > 0 ? (
          featuredProjects.map(project => (
            <ProjectCard
              title={project.data.title}
              description={project.data.description}
              category={project.data.category}
              image={project.data.image}
              link={project.data.link}
              slug={project.id}
            />
          ))
        ) : (
          <p class="empty-state">Projects coming soon!</p>
        )}
      </div>
    </div>
  </section>

  <!-- Latest Updates Section -->
  <section class="section">
    <div class="container">
      <SectionHeader 
        title="Latest Updates" 
        link="/blog"
        linkText="View All Posts"
        figNumber="02"
      />
      <div class="posts-grid">
        {latestPosts.length > 0 ? (
          latestPosts.map(post => (
            <BlogCard
              title={post.data.title}
              date={post.data.date}
              description={post.data.description}
              tags={post.data.tags}
              slug={post.id}
            />
          ))
        ) : (
          <p class="empty-state">No posts yet. Check back soon!</p>
        )}
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .posts-grid {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }
  
  .projects-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-lg);
  }
  
  @media (max-width: 768px) {
    .projects-grid {
      grid-template-columns: 1fr;
    }
  }
  
  .empty-state {
    color: var(--text-muted);
    font-style: italic;
    padding: var(--space-xl);
    text-align: center;
    background: var(--bg-secondary);
    border-radius: var(--radius-lg);
  }
</style>